// BEGIN CHAPTER 10
    /*
        Along the way we will cover the following topics
        -Basic DOM traversing
        -Accessing elements in the DOM
        -Element click handler
        -THIS and the DOM
        -Manipulating element style
        -Changing the classes of an element
        -Event listeners on elements
        -Creating new event listeners
    */

// In order to interact with our web page and create a dynamic web page, we have to connect our javascript skills to the DOM
